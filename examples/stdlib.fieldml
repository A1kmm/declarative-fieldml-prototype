# This file is really just a placeholder to let the example models
# compile; it will be improved later.

unit metre = newbase
val < :: [domain _a :: ()] => _a -> _a -> Boolean
let == < (\_x _y -> realCompare _x _y)
val >
val <=
val >=
val realCompare
# val ==
val asclone
val fromclone
val const
val +
val *
val -
